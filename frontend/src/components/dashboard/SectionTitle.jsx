export default function SectionTitle({
  title,
}) {
  return (
    <h2
      style={{
        marginBottom: "20px",
        marginTop: "8px",
        fontSize: "22px",
      }}
    >
      {title}
    </h2>
  );
}