export default function SectionTitle({ title }) {
  return (
    <h2
      style={{
        marginTop: "0",
        marginBottom: "18px",
        fontSize: "22px",
        color: "#111827",
      }}
    >
      {title}
    </h2>
  );
}